---
ns: ATTRIBUTE
apiset: client
---
## GET_DEFAULT_MAX_ATTRIBUTE_RANK

```c
// 0x7C059C55AD940CB4
int GET_DEFAULT_MAX_ATTRIBUTE_RANK(Hash modelHash, int attributeIndex);
```

## Parameters
* **modelHash**:
* **attributeIndex**: Refer to [enum: ePedAttribute]

## Return value
Returns the maximum rank that the specified `ePedAttribute` can be.
