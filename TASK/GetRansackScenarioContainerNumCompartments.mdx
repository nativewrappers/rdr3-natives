---
ns: TASK
apiset: client
nativedb: 93ddb030
---
## _GET_RANSACK_SCENARIO_CONTAINER_NUM_COMPARTMENTS

```cpp
// 0x640A602946A8C972
int _GET_RANSACK_SCENARIO_CONTAINER_NUM_COMPARTMENTS(Entity entity);
```

Returns the total number of compartments (drawers, lids, etc.) the specified scenario container entity has.
For example, a chest has 1 compartment, while a cabinet with 3 drawers returns 3.


## Parameters
* **entity**:

## Return value
Returns the total number of compartments (drawers, lids, etc.) the specific scenarion container has. For example a chest has `1` compartment, while a cabinet with `3` drawers returns `3`
