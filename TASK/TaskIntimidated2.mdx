---
ns: TASK
apiset: client
---
## _TASK_INTIMIDATED_2

```c
// 0x933ACC1A1771A288
BOOL _TASK_INTIMIDATED_2(Ped victim, Ped attacker, int p2, BOOL unusedParam3, BOOL unusedParam4, BOOL everyFrame, BOOL p6, BOOL p7, int flag);
```


## Parameters
* **victim**:
* **attacker**:
* **p2**:
* **unusedParam3**: Unused by the native.
* **unusedParam4**: Unused by the native.
* **everyFrame**: Removes the `0x1000` bitmask from `flag`
* **p6**:
* **p7**: Removes the `0x80000` bitmask from `flag`
* **flag**:

## Return value
Returns `true` on success, `false` if it failed (whether due to `attacker` being invalid, or the `attacker` ped being dead)

## Examples
<SharedExamples name="Intimidated" />
