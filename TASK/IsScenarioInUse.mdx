---
ns: TASK
apiset: client
aliases: ["0x1ACBC313966C21F3"]
---
## _IS_SCENARIO_IN_USE

```c
// 0x1ACBC313966C21F3
BOOL _IS_SCENARIO_IN_USE(Hash scenarioHash);
```

Checks whether a specified scenario is currently being used (actively played) by any entity (player or ped).

## Parameters
* **scenario**:

## Return value
Returns `true` if the scenario hash is in use, `false` otherwise.
